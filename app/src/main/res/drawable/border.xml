<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền -->
    <solid android:color="@android:color/transparent" />

    <!-- Border -->
    <stroke
        android:width="2dp"
        android:color="#E91E63" /> <!-- đổi màu viền tại đây -->

    <!-- Bo góc (optional) -->
    <corners android:radius="8dp" />
</shape>
